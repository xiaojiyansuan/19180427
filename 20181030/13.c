#include<stdio.h>
void matrix_transpose(int mat[][N], int transposed[][M])
{
	int i, j;
	for (i = 0; i < N; ++i)
		for (j = 0; j < M; ++j)
			transposed[i][j] = mat[j][i];
}
void main()
{
	int i,j;
	void matrix_transpose(int mat[i][N],int transposed[j][M]);
}
